# an EAM potential lib
version: 2

args:


pkg: "git.hpcer.dev/HPCer/CrystalMD/potential"
target: pot

dependencies:
  packages:
    github.com/google/googletest: {version: release-1.10.0, target: GTest}
